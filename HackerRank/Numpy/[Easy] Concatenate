# https://www.hackerrank.com/challenges/np-concatenate/problem

import numpy

n_size, m_size, p = map(int, input().split())
n = [list(map(int, input().split())) for _ in range(n_size)]
m = [list(map(int, input().split())) for _ in range(m_size)]

print(numpy.concatenate((n, m), axis = 0))

# Time complexity: O(n)
# Space complexity: O(n)
