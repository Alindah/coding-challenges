# https://www.hackerrank.com/challenges/zipped/problem

inp = list(map(int, input().split()))
student_id, size = list(range(1, inp[0] + 1)), inp[1]
avg_scores = []

for i in range(size):
    marks = list(map(float, input().split()))
    avg_scores += [marks]

for s in tuple(zip(*avg_scores)):
    print(sum(s) / size)
    
# Time complexity: O(n)
# Space complexity: O(n)
